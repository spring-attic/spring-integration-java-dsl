server:
  port: 0
spring:
  jmx:
#    enabled: false
  datasource:
    schema: classpath*:**/schema.sql
    data:   classpath*:**/data.sql
  data:
    mongodb:
      port: 0
  main:
    banner-mode: "off"

